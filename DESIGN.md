# My Shop Design System

## 1. Visual Theme & Atmosphere
- 따뜻하고 활기찬 동네 쇼핑 경험을 만든다: 친근하지만 가볍지 않고, 정보가 많아도 복잡하지 않게 보인다.
- 흰색 캔버스 위에 선명한 코랄 오렌지를 단일 강조색으로 사용한다.
- 상품 사진이 주인공이며 장식보다 가격, 혜택, 구매 행동을 빠르게 이해하게 한다.
- Yanolja 자료의 시각 원칙만 참고하고 해당 브랜드의 이름, 로고, 고유 문구는 사용하지 않는다.

## 2. Color Palette & Roles
- Brand Coral `#F54B1E`: 주요 구매 버튼, 할인율, 선택 상태에만 사용한다.
- Brand Pressed `#D63E14`: 주요 버튼의 누름 상태에 사용한다.
- Coral Tint `#FEEAE2`: 프로모션과 선택 영역의 옅은 배경이다.
- Canvas `#FFFFFF`: 페이지와 카드의 기본 바탕이다.
- Warm Sand `#F5EBE1`: 필터, 비활성 영역, 보조 표면에 사용한다.
- Heading `#1E1928`: 제목과 가격에 사용하는 따뜻한 짙은 색이다.
- Body `#555055`: 본문에 사용하되 이보다 흐린 색은 긴 문장에 사용하지 않는다.
- Muted `#716969`: 보조 정보의 최저 대비 색이며 반드시 16px 이상으로 표시한다.
- Critical `#C8322A`, Success `#187A56`, Info `#1769C2`: 색과 아이콘·문구를 함께 사용한다.

## 3. Typography Rules
- 기본 서체는 `Pretendard, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`다.
- 모든 본문, 가격, 버튼, 메뉴, 배지, 입력값, 도움말은 절대 `16px` 아래로 내리지 않는다.
- Display `40px/700/1.25`, H1 `34px/700/1.3`, H2 `28px/700/1.35`, H3 `22px/700/1.4`를 쓴다.
- 기본 본문은 `18px/400/1.7`, 보조 본문은 `16px/500/1.6`으로 쓴다.
- 가격과 할인은 700, 상품명은 600, 설명은 400의 세 가지 굵기만 사용한다.
- 한글과 영문을 같은 서체로 처리하고 이탤릭, 과도한 자간, 긴 대문자 표기를 피한다.

## 4. Component Styling
- 주요 버튼: `#F54B1E` 배경, 흰 글자, 높이 56px, 18px/700, 8px radius, 좌우 24px padding.
- 버튼은 한 화면에 하나만 가장 강하게 보이며 눌렀을 때 `scale(0.97)`과 `#D63E14`를 적용한다.
- 보조 버튼: 흰 배경, `2px solid rgba(30,25,40,0.22)`, 18px/700, 8px radius.
- 비활성 버튼: `#F5EBE1` 배경과 `#716969` 글자를 쓰고 모양과 크기는 유지한다.
- 상품 카드: 흰 배경, 8px radius, 사진 16:9, 내부 여백 16px, 기본 상태는 그림자 없이 둔다.
- 카드 hover는 `0 2px 8px rgba(30,25,40,0.10)`만 사용하고 카드 전체를 클릭 영역으로 만든다.
- 할인 배지: 사진 좌상단, 코랄 배경, 흰색 16px/700, 6px radius, `6px 10px` padding.
- 입력창: 높이 56px, 18px 글자, 8px radius, 명시적 라벨, focus는 3px 코랄 외곽선이다.
- 검색창 placeholder도 16px 이상이며 아이콘만으로 목적을 설명하지 않는다.
- 카테고리 칩: 최소 높이 44px, 16px/600, 기본 Sand 배경, 선택 시 Coral 배경과 흰 글자.

## 5. Layout Principles
- 간격은 4px 기반의 `8, 12, 16, 24, 32, 48, 64, 80` 체계만 사용한다.
- 콘텐츠 최대 폭은 1280px, 좌우 여백은 모바일 20px, 태블릿 28px, 데스크톱 40px다.
- 상품 그리드는 모바일 1열, 600px 이상 2열, 900px 이상 3열, 1200px 이상 4열이다.
- 카드 사이 간격은 모바일 20px, 데스크톱 24px 이상으로 유지한다.
- 상품 정보 순서는 사진 → 상품명 → 짧은 설명 → 혜택 → 가격 → 구매 행동으로 통일한다.
- 사진 위에는 할인 배지 외의 긴 문장을 겹치지 않는다.

## 6. Depth, Images & Motion
- 그림자는 기능적 계층에만 사용하며 색이 있는 그림자와 내부 그림자는 금지한다.
- 상품 사진은 밝고 자연스럽게 촬영하고 같은 카테고리에서는 비율과 배경 톤을 통일한다.
- 기본 전환은 150–240ms이며 결제·오류 화면에는 튀는 spring 효과를 사용하지 않는다.
- `prefers-reduced-motion`에서는 이동과 확대를 제거하고 즉시 전환 또는 짧은 fade만 사용한다.

## 7. Accessibility & Responsive Behavior
- 모든 클릭·터치 영역은 최소 `48×48px`, 주요 버튼은 최소 56px 높이로 만든다.
- 키보드 focus는 3px 고대비 외곽선으로 항상 보이게 하고 제거하지 않는다.
- 모바일에서도 글자 크기를 줄이지 말고 열 수와 여백만 줄인다.
- 확대 200%에서도 상품명, 가격, 수량, 구매 버튼이 겹치거나 잘리지 않아야 한다.
- 상태는 색상 하나에 의존하지 않고 Lucide 아이콘과 짧은 한국어 문구를 함께 제공한다.
- 아이콘은 `lucide-react`만 사용하고 서로 다른 아이콘 라이브러리를 섞지 않는다.
- 아이콘은 SVG와 `currentColor`를 사용하며 단독 아이콘 버튼에는 접근 가능한 이름을 제공한다.

## 8. Voice, States & Guardrails
- 문구는 짧고 친근한 존댓말 `-요` 어미를 사용하며 구매 결과와 오류 원인을 분명히 말한다.
- 빈 화면은 이유 한 문장과 다음 행동 하나를 제공한다.
- 로딩은 카드와 동일한 크기의 Sand skeleton을 사용하고 기존 콘텐츠를 갑자기 지우지 않는다.
- 결제 성공과 실패는 toast가 아닌 전용 결과 영역에서 요약과 다음 행동을 보여준다.
- 코랄을 본문, 큰 배경, 구분선에 남용하지 않고 한 화면에 강한 코랄 CTA를 두 개 두지 않는다.
- 순수 검정, 흐린 작은 글씨, 과도한 gradient, 무거운 그림자, emoji를 사용하지 않는다.
- 모든 새 화면은 큰 글씨, 높은 대비, 명확한 가격, 단순한 구매 흐름을 먼저 검증한다.
